class Solution {
public:
    int peakIndexInMountainArray(vector<int>& v) 
    {
        int n=v.size(),ans=0;
        for(int i=0; i<n; i++)
        {
            if(v[i]>v[ans]) ans=i; 
        }
        return ans;
    }
};