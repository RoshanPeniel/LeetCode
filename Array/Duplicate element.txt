class Solution {
public:
    bool containsDuplicate(vector<int>& a) 
    {
        sort(a.begin(), a.end());
        auto it = std::unique( a.begin(), a.end() );
        bool wasUnique = !(it == a.end() );
        return wasUnique;
        
        
    }
};